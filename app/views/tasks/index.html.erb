<h1>タスク一覧</h1>
<table>
  <head>
    <tr>
      <th>タスク名</th>
    </tr>
  </head>
  <body>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.title %></td>
      </tr>
    <% end %>
  </body>
</table>
<%= link_to '追加', new_task_path %>